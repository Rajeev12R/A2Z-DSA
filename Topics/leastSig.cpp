#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int num;
        int input;
        cin >> input;

        num = pow(2, input);

        cout << num << endl;
    }

    return 0;
}